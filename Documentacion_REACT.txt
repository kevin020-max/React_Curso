npm create vite@latest

REPOSITORIO GITHUB

Iniciar un repositorio local:
"git init"

Cambiar el nombre de master a main: 
"git branch -M main"

Eliminar Cedenciales de otros usuarios
"Win + s"
Escribir:"Administrador de credenciales 
Seleccionar credenciales de windows y luego buscar las cuentas GitHub asociadas y eliminar las credenciales
Luego de eliminar las credenciales

Configurar usuario local de Git:
"git config user.email "su_email"
"git config user.name "su_nombre"

Ahora agregar los archivos del proyecto
"git add ."

Realizar un commit descriptivo de la version a subir
"git commit -m "Primera subida""

Enlazar el repositorio en la nube con el local:
"git remote add origin https://github.com/kevin020-max/react_projrct.git
git push -u origin main

Luego saldrá que debe iniciar sesion con el navegador:
Inicie sesion con suus cedenciales del GitHub.
Listo actualiza el repositorio de la nube

Comprobar el estado del royecto:
"git status"

(Si aparece algo en rojo significa que el archivo es nuevo o se ha modificado).

Comandos para actualizar el repositorio con la version local:
git status
git add .
ait commit -m "nombre de la version o desripcion del commit"
git push

Crear Ramas:
git checkout -b nombre de la rama
git switch -c nombre de la rama
git push origin nombre de la rama

 git push --set-upstream origin nombre de la rama

CREAR PROYECTO REACT
Escoger react
Escoger Javascript + SWC
